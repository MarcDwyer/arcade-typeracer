export enum RouteModes {
  single = "single",
  multi = "multi",
}

export enum Phases {
  start = "start",
  waiting = "waiting",
  loaded = "loaded",
  complete = "complete",
  countdown = "countdown",
  typing = "typing",
}
