export enum RouteModes {
  single = "single",
  multi = "multi",
}

export enum Phases {
  waiting = "waiting",
  loaded = "loaded",
  countdown = "countdown",
  typing = "typing",
  complete = "complete",
}

export enum PayloadTypes {
  typing_text = "typingtext",
}
