export enum RouteModes {
  single = "single",
  multi = "multi",
}

export enum Phases {
  start = "start",
  waiting = "waiting",
  prepare = "prepare",
  loaded = "loaded",
  complete = "complete",
  countdown = "countdown",
}
