export enum RouteModes {
  single = "single",
  multi = "multi",
}

export enum Phases {
  waiting = "waiting",
  loaded = "loaded",
  complete = "complete",
  countdown = "countdown",
  typing = "typing",
}

export enum PayloadTypes {
  typing_text = "typingtext",
}
