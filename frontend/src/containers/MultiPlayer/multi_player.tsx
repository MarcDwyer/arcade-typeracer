import React from "react";
import { useDispatch } from "react-redux";

export default function MultiPlayer() {
  const dispatch = useDispatch();

  return (
    <span>this is multiplayer</span>
  );
}
